Conference Poster Prompy

Let's Restart. 
Can you: 

1. propose the layout framework (vertical direction poster) based on what we have discussed (reader/audience/what problem can you tackle centric) => 

2. this was the pipeline flow of our inisar + few shot based spatial - temporal point cloud transformer with pre-trained meta learning diagram, based on this propose in whcih section we allocate which icons & what icons we need. 

3. Note
    1. instead of saying simply "privacy concerns", I do believe that the icon there should be a cluster of "low-light cndition", "adverse weather conditions (e.g. foggy, raining)", "occlusions", then "privacy concerns (blurred facial features instead of slash to camera) . 
    2. for the "why radar" icon, i think we should illustrate the radar working theory with stack of person moving images in both indoor and outdoor environements to indicate the concept of time and reflected siganls e.g. (ToF, the signal modeling of scatters, as well as spectrum) to highlight why radar "Millimeter-wave (mmWave) radar provides a privacy-preserving alternative by avoiding facial information while maintaining robustness to environmental factor". 
    3. Then we illustrate on the regular/conventional radar processing techniques, or radar-based person identification problems, such as 
        1. "First, conventional angle-of-arrival processing produces sparse and noisy point clouds with limited angular resolution, motion-induced defocusing, and multipath ghost artifacts, particularly in indoor environments "
        2. in particular " 3D Radar Imaging & Point Cloud Deep Learning.: Early radar-based PID approaches relied on micro-Doppler spectrograms, which capture motion signatures but lack explicit 3D geometric structure. Interferometric ISAR (InISAR) techniques have demonstrated motion compensation, CLEAN autofocusing, and interferometry for large rigid targets such as ships and aircraft. However, these methods were developed for extended, rigid scatterers and have not been systematically validated on small, articulated human targets, where body-part motion, low-radar cross-section segments, and multipath reflections introduce severe reconstruction challenges. In particular, indoor environments generate ghost clusters and aspect-dependent sparsity that can significantly distort point cloud structure, limiting direct adoption of classical InISAR pipelines for human PID." & 
        3. "Second, existing learning-based methods require extensive labeled data (often >100 sequences per individual, corresponding to several hours of recording), which limits scalability and makes adaptation to new subjects from few examples impractical" where on top of that "On the learning side, PointNet introduced permutation-invariant processing of point clouds via shared MLPs andmax-pooling, but does not explicitly model local geometric relationships. The Point Cloud Transformer (PCT) improves spatial representation through neighbor embedding and offset-attention, enabling better capture of local structures. Nevertheless, most prior PCT-based studies focus on static object recognition, whereas radar PID requires handling temporally evolving, sparse, and noise-corrupted point clouds." . 
        4. In the following, possibly we need icons to illustrate "how We address these two challenges through a unified framework that combines tracking-integrated Interferometric ISAR (InISAR) with pre-trained meta-learning. Rather than treating sensing and learning independently, we jointly improve radar point cloud quality and few-shot generalization. (the outdated pipeline diagram that we attached)", which in detail outlines our key contributions in three-folds (hierarchy layout, layered diagram): 
            1. "(1) Radar domain: We develop a tracking-integrated InISAR pipeline that reconstructs high-resolution 3D point clouds of articulated human motion while mitigating multipath artifacts in indoor environments, and validate its performance against motion capture data." & 
            2. "(2) Machine learning domain: We introduce a spatio-temporal transformer architecture (PCTtrans) embedded in Model-Agnostic Meta-Learning (MAML), and show that task-specific pre-trained initialization substantially improves few-shot adaptation for radar point cloud person identification." & 
            3. "(3) System-level integration: We demonstrate that combining high-quality 3D radar reconstruction with meta-learning enables practical few-shot radar-based PID, reducing the dependence on large subject-specific training datasets". 
        5. Note that here we should not show the details of the model architects. and we explicitly highlight that this resolves the following additional integrated problem shown in the diagram, icons, or just bullet point text "Radar PID & Meta-Learning. Recent radar PID systems combine PointNet-style encoders with temporal convolutions or Transformers to capture gait dynamics. Despite improved temporal modeling, these methods require large subject-specific datasets and show near-random performance on unseen individuals. Model-Agnostic Meta-Learning (MAML) enables rapid task adaptation and has been explored for static point cloud tasks. Most existing studies initialize MAML randomly, whereas work in NLP indicates that pre-trained initialization can significantly accelerate meta-learning. We extend this idea to radar point cloud PID, combining task-specific pre-training with meta-learning to improve few-shot generalization". 
        6. Specifically, in the signal processing part "FMCW radar measures range and Doppler via FFT along fast-time (within chirp) and slow-time (across chirps). ISAR synthesizes larger apertures using target motion to achieve cross-range resolution. Motion compensation removes phase offsets caused by articulated body motion to focus scattered energy. InISAR extends ISAR to 3D using phase differences between spatially separated antennas for elevation estimation. Radar point clouds exhibit: (1) Aspect-Dependent Density—count varies with orientation and range attenuation (∝ 1/r4), creating dense torso clusters and sparse limbs. (2) Multipath Noise—indoor reflections produce ghost clusters. (3) Near-Field Errors—height estimation degrades within 4m due to phase unwrapping ambiguities. (4) Data Scarcity—methods require >100 sequences per person". 
    4. At this point, we can display in the center the system architecture (InISAR + few-shot PCTtrans + pretrained meta learning) diagram (i will upload a reference diagram i made years ago that might not match the style), but again, not yet the details of the learning model architecture. 
    5. THEN, in the next secion we should show two sections of diagrams, one for the InISAR pipeline part and one for the deep learning part, such that in this section we can present the decomposed pipeline and respective components icons as well as some key formulas. 
    6. In the InISAR section, we should have the stacked snapshots of noisy and sparse point clouds again to indicate the time concept and radar point cloud challenges as input in 2d or 3d. We should also include the spectrogram diagram (range Doppler profile) to indicate the FFT process, the MIMO antenna layout diagram ( i will also upload a reference diagram again i made years agao that might not match the style), and the CFAR theory icons (lagging and leading cells, center cell, guard cell, trainign cells of range doppler profiles). 
        1. The pipeline diagram of the InISAR pipleine could be sperately shown. 
        2. The 3D point cloud via the InISAR pipeline "reconstructs high-resolution 3D point clouds through six stages: 
            1. (1) Range Processing—Fast-time FFT. 
            2. (2) ISAR Motion Compensation—Image Contrast-Based Autofocusing (ICBA) estimates and removes motion-induced artifacts. 
            3. (3) Doppler processing—Slow-time FFT. 
            4. (4) CFAR Segmentation—Smallest-Of Cell-Averaging Constant False Alarm Rate (SOCA-CFAR) segments target regions. 
            5. (5) Interferometry—Phase-based 3D reconstruction. 
            6. (6) Multi-Target Tracking— Extended Kalman Filter (EKF) and joint probabilistic data association (JPDA) jointly mitigate multipath effects." 
    7. In the learning section, we should show a separate learning pipeline with again stacked in time axis point clouds ( i will also upload a reference diagram again i made years agao that might not match the style) , as well as we shoud decompose the model layers and show it in a style taht natches our poster ( i will also upload a reference layer model decomposition diagram which i think is clear but might not match the style), as well as ht eky components in how input embeddings are mapped and flowed throughout the process ( i will also upload a reference diagram again i made years agao that might not match the style) . A few-shot icon indicating new samples from not learned domain knowledge, and a pretrained meta-learning icon that indicates the pretrained domain knowledge as well as the learning/adaptation gradient arrows, should also be included. 
    8. THEN after these major section we can move on to the result sections showing bar results or some format that you suggest, which would highlight the improvements. along with bullet texts highlighting our contributions concisely and summarizing significant results. 
    9. Along with a tabular visual might be nice. We should also provide a QR code for the audience to scan to watch the (1) inisar-processed 3d piont cloud video generated using ffpmeg (2) the difference DBSCAN can make to our non-tracking integrated Inisar point cloud videos, again generated using ffpmeg. (3) we dont have properly scaled tracking-integrated INISAR 3d piont cloud videos, as it took too long to do the process at the time. 
        1. At the very last, we should include a section of the obstacles that we find if we were to employ these techniques in the industry. For instance, 
            1. (1) this is currently offline processing as INISAR + particle-like (each scatter trajectory prediction) consumes major time. online version, or track only the clustered centers, should be adapted for industrial adaptation to shorten time while maintaining effectiveness 
            2. (2) A multimodal version of this system would definitely perform better if there is no privacy concern. Although mmwave radar is reliable in adverse environment settings, it still lacks of RGB information that can precisely differentiate between person with their facial features. Gait information, if used to distinguish between persons in a large population settings, would not be sufficient. 
            3. (3) it is possible and maybe worthy to generate multiple pretrained learning weights as public datasets for future community development, but that would also require more people samples and better hardwares such as clusters of HPC whcih was our pain point. 
            4. (4) Improvements in tracking could definitely be made to adapt to industrial online settings to significantly improve the speed of processing. 
            5. (5) The maximum missing frame used in the current JDPA-EKF tracking is not optimized and can be tuned to adapt to different settings, such as settings with a higher occlusion probability. The maximum missing frame threshold should be increased, as the current setting is 1 frame only. vice versa. Or we can even make it self-adaptive.
