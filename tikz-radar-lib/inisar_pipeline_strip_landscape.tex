\documentclass[tikz, border=10pt]{standalone}
\usepackage{xcolor}
\usepackage{amsmath}
\renewcommand{\familydefault}{\sfdefault}

% ==============================================================
% INPUT ALL LIBRARY FILES
% ==============================================================
\input{radar_styles.tex}
\input{radar_icons.tex}
\input{geometry_icons.tex}
\input{tracking_icons.tex}

\begin{document}
\begin{tikzpicture}[
    node distance=0.9cm,
    every node/.style={align=center}
]

% ======================================================================
% ROW 1: RADAR SIGNAL PROCESSING CHAIN
% ======================================================================
\ProcessCard[card color=radarProcessing]{p1}{Raw Radar}{\RadarSensor}
\ProcessCard[right=1.0cm of p1, card color=radarProcessing]{p2}{Range FFT}{\RangeFFT}
\ProcessCard[right=1.0cm of p2, card color=radarProcessing]{p3}{Motion Comp.}{\MotionCompensation}
\ProcessCard[right=1.0cm of p3, card color=radarProcessing]{p4}{Doppler FFT}{\RangeDoppler}
\ProcessCard[below=1.8cm of p3, card color=radarProcessing]{p6}{Interferometry}{\Interferometry}
\ProcessCard[left=1.0cm of p6, card color=radarProcessing]{p5}{CFAR}{\CFAR}
\ProcessCard[right=1.0cm of p6, card color=radarProcessing]{p7}{EKF-JPDA}{\EKFJPDA}
\ProcessCard[right=1.0cm of p7, card color=geometryColor]{p8}{3D Point Cloud}{\PointCloudHuman}

% Flow arrows (Row 1)
\foreach \s/\t in {p1/p2, p2/p3, p3/p4}{
    \draw[flow] (\s) -- (\t);
}
\draw[flow, dashed, color=radarProcessing] (p4.south) -- ($(p4.south) + (0,-0.8)$) -| (p5.north);
\foreach \s/\t in {p5/p6, p6/p7, p7/p8}{
    \draw[flow, color=radarProcessing] (\s) -- (\t);
}

% ======================================================================
% KEY EQUATIONS (Bottom)
% ======================================================================
\node[draw=iconStroke, fill=iconFaint, rounded corners=2pt,
      minimum width=2.8cm, minimum height=0.9cm, font=\scriptsize,
      text=radarNavy, align=center, anchor=north] (eq1) at ($(p3.west)+(-3.75,-1.45)$)
    {$\hat{\beta},\hat{\gamma} = \arg\max\ \text{contrast}$};

\node[draw=iconStroke, fill=iconFaint, rounded corners=2pt,
      minimum width=2.8cm, minimum height=0.9cm, font=\scriptsize,
      text=radarNavy, align=center, anchor=south] (eq2) at ($(p6.north east)+(2.5,0.3)$)
    {$h = \dfrac{\lambda \phi R}{2\pi L}$};

% Connect equations to pipeline stages
\draw[iconFaint, line width=0.3pt] (eq1) -- (p3);
\draw[iconFaint, line width=0.3pt] (eq2) -- (p6);

\end{tikzpicture}
\end{document}